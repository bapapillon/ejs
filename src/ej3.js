/*
 * Ejercicio 3:
 * Completar la función `obtenerElMinimoDeLaLista(lista)`,
 * que recibe un array de números y retorna el valor mínimo.

 * Ejemplo: obtenerElMinimoDeLaLista([3, 1, 2]) => 1
 * Además, si la lista que recibe está vacía, deberá retornar `null`: obtenerElMinimoDeLaLista([]) => null

 * TEST: `npm test -- ej3`
 **/

function obtenerElMinimoDeLaLista(lista) {
  // COMPLETAR
}

// <--- NO TOCAR! --->
module.exports = obtenerElMinimoDeLaLista;
